# Python
.venv/
__pycache__/
*.pyc

# IDE
.vscode/
.ipynb_checkpoints/

# Runtime / generated
logs/
experiments/
cache/

# Secrets and environment
.env
.env.*
*.key
*.pem
*.p12
secrets.json
credentials.json

# Local-only connection details (SSH passphrases, hosts). Never commit.
.ssh_local/
*.passphrase
ssh_local.txt

# Cached data
data/gtfs
data/networks
data/osm_cache
data/pygris_cache
# HPMS geometry/truck-share responses, re-fetched on demand and cached for
# freight.hpms.cache_days. One 15-county bbox is ~17 MB of ArcGIS JSON.
data/hpms

# Databases — never commit these. They are large and carry real data.
*.duckdb
*.duckdb.wal
data/db/
jetstream_server_db/

# Stray/nested duckdb copies and lock files
data/db/db/
*.duckdb.lock

# Local scratch artefacts (leading underscore marks a throwaway)
data/_*
*.tmp


REVIEW_ACTION_ITEMS.txt
private_docs/
